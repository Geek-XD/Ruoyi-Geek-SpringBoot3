# local配置
local:
  enable: true
  primary: MASTER
  client:
    MASTER:
      permission: public
      path: /data/files/master
      api: /file/download/resource
    # SLAVE:
    #   permission: public
    #   path: /data/files/slave
    #   api: 

# Minio配置
minio:
  enable: false
  primary: MASTER
  client:
    MASTER:
      permission: public
      url: http://localhost:9000
      accessKey:
      secretKey:
      bucketName: ruoyi
    # SLAVE:
    #   permission: public
    #   url: http://127.0.0.1:9000
    #   accessKey:
    #   secretKey:
    #   bucketName: ry

# oss配置
oss:
  enable: false
  primary: MASTER
  client:
    MASTER:
      permission: public
      accessKeyId: accessKeyId
      accessKeySecret: accessKeySecret
      bucketName: ruoyi
      endpoint: oss-cn-beijing.aliyuncs.com
    # SLAVE:
    #   permission: public
    #   accessKeyId: 
    #   accessKeySecret: 
    #   bucketName: 
    #   endpoint:


